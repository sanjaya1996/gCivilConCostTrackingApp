class MiniPhase {
  constructor(id, phaseId, title, status, description) {
    this.id = id;
    this.phaseId = phaseId;
    this.title = title;
    this.status = status;
    this.description = description;
  }
}

export default MiniPhase;
