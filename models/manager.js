class Manager {
  constructor(id, firstName, lastName, email, phoneNumber, supervisorId) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.email = email;
    this.phoneNumber = phoneNumber;
    this.supervisorId = supervisorId;
  }
}

export default Manager;
